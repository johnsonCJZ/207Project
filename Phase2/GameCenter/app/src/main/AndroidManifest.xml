<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fall2018.csc2017.slidingtiles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainSlideActivity" />
        <activity android:name=".GameStoreActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SlideDifficultyActivity" />
        <activity
            android:name=".MainInfoPanelActivity"
            android:label="@string/title_activity_main_info_panel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Main2048Activity" />
        <activity android:name=".ScoreBoardTabLayoutActivity" />
        <activity android:name=".MineSweeperActivity" />
        <activity android:name=".MinesweeperDifficultyActivity" />
        <activity android:name=".CreateNewOrLoad2048Activity"></activity>
    </application>

</manifest>